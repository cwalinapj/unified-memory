Unified Memory is a content-addressed, claim-based memory format with append-only history hashing and Merkle-batched attestations, enabling multi-agent recall with confidence, provenance, and cryptographic verification.
